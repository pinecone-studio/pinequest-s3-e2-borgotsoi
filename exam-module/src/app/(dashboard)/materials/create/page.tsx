import CreateMaterialPage from "./CreateMaterialPage";

/** Avoids intermittent Next.js PageNotFoundError (ENOENT) during static page collection. */
export const dynamic = "force-dynamic";

export default CreateMaterialPage;
